<form method="post" action="{{ path('grath_delete', {'id': grath.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ grath.id) }}">
    <button class="btn">Delete</button>
</form>
